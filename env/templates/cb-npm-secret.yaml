{{- if .Values.cbNexusToken }}
{{- if .Values.cbNexusUsername }}
apiVersion: v1
data:
  file: {{ "//nexus-internal.cloudbees.com/repository/npm-releases//:_authToken=" | b64enc }}{{ (printf "%s:%s" .Values.cbNexusUsername .Values.cbNexusToken) | b64enc }}
kind: Secret
metadata:
  name: cb-npm-token
type: Opaque
{{- end }}
{{- end }}
