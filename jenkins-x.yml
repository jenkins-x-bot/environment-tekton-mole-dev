buildPack: none
pipelineConfig:
  pipelines:
    release:
      pipeline:
        stages:
          - name: release
            steps:
              - name: helm-build
                image: rawlingsj/builder-go:wip1
                command: jx
                workingDir: env
                args: ['step','helm','apply']
