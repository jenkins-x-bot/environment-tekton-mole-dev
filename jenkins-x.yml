buildPack: none
pipelineConfig:
  pipelines:
    release:
      pipeline:
        agent:
          image: rawlingsj/builder-go:wip2
        stages:
          - name: release
            steps:
              - name: helm-build
                image: rawlingsj/builder-go:wip2
                command: jx
                args: ['step','helm','apply','--dir','/workspace/source/env']
