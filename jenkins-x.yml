buildPack: none
pipelineConfig:
  pipelines:
    release:
      pipeline:
        options:
          containerOptions:
            workingDir: env 
        agent:
          image: rawlingsj/builder-go:wip2
        stages:
          - name: release
            steps:
              - name: helm-build
                image: rawlingsj/builder-go:wip2
                command: jx
                workingDir: env
                args: ['step','helm','apply']
